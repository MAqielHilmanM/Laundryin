<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.creative.iam.laundryin">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.order.confirm.ConfirmPesananActivity"></activity>
        <activity android:name=".views.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.order.detail.DetailPesananActivity" />
        <activity android:name=".views.main.UtamaActivity" />
        <activity android:name=".views.notification.NotifikasiActivity" />
        <activity android:name=".views.order.success.PesanBerhasilActivity" />
        <activity android:name=".views.paket.PaketActivity" />
        <activity android:name=".views.registrasi.SignUpActivity" />
        <activity
            android:name=".views.login.LoginActivity"
            android:label="@string/title_activity_login"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".views.onboard.OnboardActivity" />
    </application>

</manifest>